# نگاشت صفحات دوره به داده‌های پروژه

مرجع: فایل Music-App، بخش `1127:12320`.

ساختار بخش از Figma دریافت شد؛ درخواست جزئیات زیرصفحه‌ها با خطای سقف سهمیه Starter مواجه شد. این سند موجودی اولیه است، نه تأیید پوشش تمام المان‌ها. برای تکمیل رابط و تعیین فیلدهای دقیق، جزئیات صفحات یا خروجی تصویری آن‌ها لازم است.

| صفحه / المان شناسایی‌شده | معادل فعلی | نیاز شناسایی‌شده |
| --- | --- | --- |
| فهرست دوره و کارت دوره | `creator_courses`؛ عنوان، توضیحات، قیمت، جلد، مدرس | تطبیق اجزای کارت با جزئیات طرح |
| جست‌وجو و حالت بدون نتیجه | فهرست دوره در API؛ جست‌وجوی محدود در خانه | جست‌وجو و نمایش نتیجه در صفحه دوره‌ها |
| فیلتر امتیاز | فیلد یا سرویس امتیاز دوره در CourseMarket وجود ندارد | امتیاز هر کاربر، شمارش رأی و میانگین محاسبه‌شده |
| فیلتر مدت ویدیو | `creator_course_media` فقط mime و حجم دارد | مدت رسانه و مجموع مدت دوره |
| فیلتر دسته‌بندی | اتصال دسته‌بندی به `creator_courses` وجود ندارد | بررسی استفاده مجدد از دسته‌بندی سایت و جدول ارتباط دوره |
| فیلترهای انتخاب‌شده و حذف فیلتر | وضعیت رابط | نیازی به ستون دیتابیس ندارد؛ پارامتر جست‌وجوی API کافی است |
| پیش‌نمایش ویدیو | پخش فایل درس پس از بررسی دسترسی | تعیین ویدیوی معرفی عمومی جدا از درس‌های خصوصی |
| عنوان، مدرس، قیمت و ثبت‌نام | `creator_courses`، `social_profiles`، `creator_course_orders` | استفاده مجدد از مدل خرید و دسترسی موجود |
| فصل‌ها و درس‌ها | `creator_courses.curriculum` و `creator_course_lessons` | تطبیق فهرست، زمان درس و وضعیت باز/قفل با طرح |
| متن درس و رمز | `posts` خصوصی، `translations`، `creator_lesson_access` | حفظ ساختار خصوصی موجود |
| پیشرفت درس | `social_lesson_progress` | استفاده مجدد؛ عدم ذخیره درصد تکراری |
| ذخیره دوره | `social_bookmarks` | استفاده مجدد |
| پسندیدن و نپسندیدن دوره | واکنش اجتماعی فعلی به `social_posts` متصل است | ارتباط مستقل واکنش کاربر با دوره؛ یک رأی فعال برای هر کاربر |
| گزارش مشکل | سرویس گزارش دوره وجود ندارد | گزارش، گزارش‌دهنده، متن، وضعیت رسیدگی و زمان |
| اشتراک‌گذاری | URL صفحه دوره موجود است | عمل رابط؛ ستون جدید لازم نیست مگر آمار اشتراک در طرح خواسته شده باشد |
| خلاصه و درباره دوره | توضیحات کلی موجود است | استخراج دقیق عنوان‌ها، فهرست‌ها و سایر مشخصات از طرح |
| برنامه‌ریزی زمان یادگیری | معادل اختصاصی دوره وجود ندارد | برنامه شخصی کاربر و دوره؛ جزئیات تکرار/زمان پس از مشاهده طرح |
| اطلاعات مدرس | `users` و `social_profiles` | استفاده مجدد؛ بررسی فیلدهای تخصصی طرح |
| پرسش‌وپاسخ و پاسخ به سؤال | دیدگاه سایت به `posts` متصل است، نه دوره | ارتباط سؤال با دوره، نویسنده، پاسخ والد و وضعیت نمایش |
| ثبت نظر، رأی و تأیید موفقیت | سرویس اختصاصی نقد دوره وجود ندارد | نقد کاربر و دوره، جزئیات رأی، متن و محدودیت ارسال تکراری |
| منابع و لینک‌ها | فایل‌های موجود به درس متصل‌اند | مدل منابع دوره، عنوان، نوع، فایل یا URL و ترتیب نمایش |
| دوره‌های مرتبط و قبل/بعد | داده دوره موجود است | انتخاب بر اساس دسته‌بندی و ترتیب؛ معمولاً داده مشتق‌شده است |
| تم، زبانه‌ها، دکمه بازگشت به بالا | وضعیت رابط | نیازی به ستون دیتابیس ندارد |

## صفحات لازم برای بررسی جزئیات

- فیلترها: `1127:12322`
- فهرست دوره‌ها: `1127:12612`
- بدون نتیجه: `1127:12818`
- صفحه فصل‌ها: `1127:13156`
- درباره دوره: `1127:13784`
- پرسش‌وپاسخ: `1127:14083`
- منابع (در فایل با نام Review Screen): `1127:14265`
- فرم ارسال نظر و حالت موفقیت: زیرصفحه‌های بخش Course Review
- پیش‌نمایش افقی: `1127:13570`

تا پیش از دریافت جزئیات، هیچ مهاجرت جدیدی برای این درخواست اجرا نشده است. نام و نوع فیلدهای تازه باید با داده‌های واقعی طرح نهایی شوند؛ عناصر صرفاً نمایشی نباید به ستون‌های غیرضروری تبدیل شوند.
